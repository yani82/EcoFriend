<%= form_for ([@brand, @comment]) do |f| %>
<div class="field <%= 'field_with_errors' if @comment.errors[:review].any? %>">
<br>
<%= f.text_area :review, class: "commentBox", style: "width: 65%", placeholder:"add a comment" %>
<br> 
<%= f.submit({:class => 'btn btn-primary'})%>
</div>
<% end %>