<%= form_for ([@brand, @comment]) do |f| %>
<div class="field <%= 'field_with_errors' if @comment.errors[:content].any? %>">
<%= f.text_field :review, class: "commentBox", style: "width: 65%", placeholder:"add a comment" %>
<%= f.submit "Submit Review"%>
</div>
<% end %>